<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sahaj.mathlearns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".addLevOne"></activity>

        <activity android:name=".addLevTwo"></activity>

        <activity android:name=".subLevOne"></activity>

        <activity android:name=".subLevTwo"></activity>

        <activity android:name=".multLevOne"></activity>

        <activity android:name=".multLevTwo"></activity>

         <activity android:name=".divLev"></activity>

        <!-- <activity android:name=".divLevtwo"></activity> -->
        <activity android:name=".mainMenu"></activity>
        <activity android:name=".Day_Test_Class">
            <intent-filter>
                <action android:name="android.intent.action.RUN"></action>
            </intent-filter>
        </activity>

    </application>

</manifest>